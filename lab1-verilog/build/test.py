#! /usr/bin/python

import sys
import string

if len(sys.arvg) < 2:
  print "usage: %s <file>" % sys.argv[0]
  sys.exit(1)

file = sys.argv[1]
f = open(file)
lines = map(string.strip, f.readline())
f.close()

for line in lines:
  print line

for line in lines:
  str = line.split()
  for str in strs:
    print str

for i,line in enumerate(lines):
  strs = lines[i+2].split()

alist.append
print alist
for aelement in alist
  print aelement
def mu12(x):
  return 2*x


